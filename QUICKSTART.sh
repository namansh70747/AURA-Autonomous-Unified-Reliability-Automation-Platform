#!/bin/bash

# AURA - Quick Reference Guide
# This file shows you exactly which script to run

cat << 'EOF'

╔═══════════════════════════════════════════════════════════╗
║              🧠 AURA - QUICK START GUIDE                 ║
╚═══════════════════════════════════════════════════════════╝

📌 RECOMMENDED: Use start.sh for the first time setup

┌───────────────────────────────────────────────────────────┐
│  🚀 PRIMARY SCRIPT (Recommended)                          │
├───────────────────────────────────────────────────────────┤
│  ./scripts/start.sh                                       │
│  → Complete setup and start AURA                          │
│  → Checks prerequisites (Go, Docker, kubectl)             │
│  → Builds AURA                                            │
│  → Starts all services (Docker)                           │
│  → Deploys Kubernetes resources (if available)            │
│  → Enables Kubernetes pod watcher & scraper               │
│  → Verifies all endpoints                                 │
│  → Launches AURA with full monitoring                     │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  📋 MAKEFILE COMMANDS                                     │
├───────────────────────────────────────────────────────────┤
│  make start         → Same as ./scripts/start.sh          │
│  make run           → Run AURA (services must be up)      │
│  make docker-up     → Start Docker services only          │
│  make build         → Build AURA binary only              │
│  make k8s-start     → Start with Kubernetes               │
│  make health        → Check system health                 │
│  make test-all      → Test all endpoints                  │
│  make clean         → Clean build artifacts               │
│  make help          → Show all commands                   │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  🎯 WHICH SCRIPT TO RUN?                                  │
├───────────────────────────────────────────────────────────┤
│  First time setup:                                        │
│    → ./scripts/start.sh  OR  make start                   │
│    → Automatically detects and enables Kubernetes         │
│    → Sets up all services and checks endpoints            │
│                                                            │
│  Services already running:                                │
│    → make run  OR  ./bin/aura                             │
│                                                            │
│  With Kubernetes only:                                    │
│    → make k8s-start                                       │
│    → Deploys to K8s and starts with scraper               │
│                                                            │
│  Just start Docker services:                              │
│    → make docker-up                                       │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  ☸️  KUBERNETES SCRAPER FEATURES                          │
├───────────────────────────────────────────────────────────┤
│  The start.sh script automatically:                       │
│  ✓ Checks for kubectl and minikube                        │
│  ✓ Starts minikube if needed                              │
│  ✓ Deploys RBAC configuration                             │
│  ✓ Deploys test applications                              │
│  ✓ Enables Kubernetes pod watcher                         │
│  ✓ Collects pod metrics every 30 seconds                  │
│  ✓ Monitors pod lifecycle events                          │
│  ✓ Stores metrics in PostgreSQL                           │
│                                                            │
│  Endpoints with Kubernetes data:                          │
│  • GET /api/v1/events      - Pod lifecycle events         │
│  • GET /api/v1/metrics/:service - Service metrics         │
│  • GET /api/v1/decisions   - Automated decisions          │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  📊 ACCESS SERVICES                                       │
├───────────────────────────────────────────────────────────┤
│  AURA API:       http://localhost:8081                    │
│  AURA Health:    http://localhost:8081/health            │
│  Prometheus:     http://localhost:9090                    │
│  Sample App:     http://localhost:8080                    │
│  PostgreSQL:     localhost:5432 (aura/aura123)           │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  📁 CLEAN PROJECT STRUCTURE                               │
├───────────────────────────────────────────────────────────┤
│  cmd/aura/           - Main application entry             │
│  internal/           - Core business logic                │
│    ├── actuator/     - Action execution                   │
│    ├── analyzer/     - Anomaly detection                  │
│    ├── core/         - Configuration                      │
│    ├── decision/     - Decision engine                    │
│    ├── learner/      - ML & optimization                  │
│    ├── observer/     - Metrics collection                 │
│    ├── predictor/    - Forecasting                        │
│    └── storage/      - Database layer                     │
│  configs/            - YAML configurations                │
│  deployments/        - Docker & K8s manifests             │
│  scripts/            - Shell scripts                      │
│    └── start.sh      - Main startup script ⭐            │
│  Makefile            - Build & deployment commands        │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│  🗑️  CLEANED UP (Removed)                                │
├───────────────────────────────────────────────────────────┤
│  ✗ scripts/demo.sh           (empty stub)                 │
│  ✗ scripts/setup.sh          (merged into start.sh)      │
│  ✗ scripts/quickstart.sh     (merged into start.sh)      │
│  ✗ pkg/utils/helpers.go      (empty stub)                │
│  ✗ internal/core/engine.go   (empty stub)                │
│  ✗ internal/adapters/*       (empty stubs)               │
└───────────────────────────────────────────────────────────┘

EOF
